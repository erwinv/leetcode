{
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "jest",
    "build:watch": "tsc -w",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "typescript": "^4.7.4"
  }
}
